id: test-sequential-workflow
name: Test Sequential Workflow
description: Test workflow with research agent using web search
type: sequence
nodes:
  - id: node-1
    agent_ref: researcher-agent
    task: "Search for the latest news about LangChain and LangGraph frameworks"
    dependencies: []
  - id: node-2
    agent_ref: researcher-agent
    task: "Based on the previous search results, summarize the key features of LangGraph"
    dependencies:
      - node-1
